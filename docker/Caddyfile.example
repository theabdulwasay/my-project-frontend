# Example Caddy config on the HOST (not inside Docker).
# Install Caddy on Ubuntu, save as /etc/caddy/Caddyfile, adjust domain.

yourdomain.com, www.yourdomain.com {
    reverse_proxy 127.0.0.1:8000
    encode gzip
}
